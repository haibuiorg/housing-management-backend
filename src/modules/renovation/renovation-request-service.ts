import { Request, Response } from "express";

export const createRenovationRequest = async (
  request: Request,
  response: Response
) => {};

export const getRenovationRequests = async (
  request: Request,
  response: Response
) => {};

export const getRenovationRequest = async (
  request: Request,
  response: Response
) => {};

export const editRenovationRequest = async (
  request: Request,
  response: Response
) => {};

export const deleteRenovationRequest = async (
  request: Request,
  response: Response
) => {};
